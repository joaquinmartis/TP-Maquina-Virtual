#include "TDAMEMORIAPRINCIPAL.h"


void cargaCodigo(T_VecMemoriaPriPal MemoriaPrincipal,T_TDDS* TDDS,int* IP){
/*Carga a la MemoriaPrincipal el codigo del programa,
a TDDS(Tabla de descriptores de segmentos) los indicadores de inicio
y fin del codigo, a IP le asigna la posicion en memoria de la primera instruccion a ejecutar siguiente*/
FILE* arch;
unsigned int tamSeg=0;


fopen()
fseek()
fread()
for
    fread()
fclose

//Actualiza TDDS


}

void MuestraMemoriaCargada(T_VecMemoriaPriPal MemoriaPrincipal){ //Muestra todo el codigo que se copio en el vector memoria, deberia poder mostrar todo en hexa
}
void almacenaValor(int op1,int valor,T_VecMemoriaPriPal MemoriaPriPal,T_Registros* Registros){
}
